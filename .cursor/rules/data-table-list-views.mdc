---
description: 列表頁資料表—TanStack ColumnDef meta、三區工具列、useClientDataTable 與 DataTableFeature 元件
globs:
  - "**/views/**/*.vue"
  - "**/data-table/**/*.vue"
  - "**/useClientDataTable.ts"
  - "**/types/data-table.ts"
alwaysApply: false
---

# 列表頁資料表（Client-side）

詳細說明與檢查清單見 **`docs/data-table-list-views.md`**。摘要：

## 必遵守

- **單一真相**：欄位以 **`ColumnDef[]`** 定義；搜尋／分面篩選／欄位標籤由 **`columnDef.meta`**（`label`、`filter`、`searchable` 提示）與 `id` 驅動，禁止與欄位無關的孤兒篩選 UI。
- **標準組合**：`useClientDataTable` + **`DataTableFeatureToolbar`** + **`DataTableFeatureSection`** + **`DataTablePagination`**（無勾選欄時 `hide-selection-info`）。
- **三區開關**：`TableListFeatures` — `search`、`filtersAndSort`、`columnVisibility`；型別見 **`src/types/data-table.ts`**。
- **分面篩選**：`meta.filter.type === 'faceted'` 時欄位需 **`filterFn: 'arrIncludesSome'`**（或等價）；**日期區間**用 `dateRange` + 自訂 `filterFn`。
- **表頭排序**：可排序資料欄使用 **`DataTableColumnHeader`**；勾選與操作欄 **`enableSorting: false`**、**`enableHiding: false`**。
- **版面**：對齊 **`ui-ux-principles.mdc`**、**`table-scroll-layout.mdc`**；**`DataTableFeatureToolbar` 在 `rounded-lg border bg-card p-4` 外**（同檔案管理頁）；**僅**表格區塊（載入、表身、分頁、空狀態）用該殼層；**勿**再套更大一層把說明＋工具列＋表整包；`DataTableFeatureSection` 內不重複畫與殼層重疊的邊框。
- **新增／上傳／建立（每個 table 列表幾乎都有）**：
  - **位置**：一律放在 **`DataTableFeatureToolbar` 的 `#actions`** slot，由 **`DataTableToolbarShell`** 排在 **重設 → 多欄排序 → 欄位顯示** 之後，成為該列 **最右側**；勿塞進 `#filters`、勿手動插在排序／欄位顯示元件之前。
  - **尺寸**：該主要按鈕使用 **`size="sm"`**，與工具列搜尋框等 **`h-8`** 控制項視覺對齊。
  - **有列勾選時**：`DataTableFeatureToolbar` 預設 **`collapseWhenRowSelection: true`** — 隱藏搜尋／篩選／多欄排序／欄位顯示；**「重設」**僅給篩選用（`useClientDataTable` 的 **`hasActiveFilters`** 不含 `rowSelection`）；頁面側 **無選取時** 才顯示「新增」、**有選取時** 只顯示「已選 + ButtonGroup」等批次區（見 **`ProjectMembersView`**）。
- **主題**：**`theme-support.mdc`**，禁止硬編碼背景／邊框色作為主結構色。

## 參考實作

- **`src/views/contract/ProjectMembersView.vue`**（正式頁面、建議複製結構；三區全开）。
- **`src/views/files/FileManagementView.vue`**、**`src/views/files/FileFormsView.vue`**、**`src/views/construction/ConstructionValuationsListView.vue`**、**`src/views/construction/PccesImportsListView.vue`**、**`src/views/construction/ConstructionDrawingsView.vue`**（樹狀表身自訂；輕量 `useClientDataTable` 僅綁 **`globalFilter`** + **`filteredFlattenedList`**；**僅全文搜尋**：`search: true`，`filtersAndSort`／`columnVisibility` 為 `false`）。
- **`src/views/construction/ConstructionDailyLogsListView.vue`**（**搜尋 + 填表日期區間**：`search: true`、`filtersAndSort: true`、`columnVisibility: false`；`logDate` 欄 `meta.filter.type === 'dateRange'`）。
- **`src/views/construction/ConstructionDefectsView.vue`**（**搜尋 + 分面 + 日期區間**：`search: true`、`filtersAndSort: true`、`columnVisibility: false`；`status`／`priority` 為 `faceted`，`updatedAt` 為 `dateRange`；列勾選與批次刪除）。
- **`src/views/repair/ProductRepairDataTableTemplateView.vue`**（示範；新頁優先採 meta 驅動之 Feature 工具列）。
