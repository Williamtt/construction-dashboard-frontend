# 前端正式環境部署（Vercel）

正式環境前端從 **prod** 分支部署至 Vercel。

**完整步驟**（含後端 Railway、R2）請見 **後端 repo** 的：

- **`docs/deployment-setup-guide.md`** — Railway、Vercel、Cloudflare R2 逐步架設教學

其中 **第二章** 為 Vercel 操作：連線 GitHub、選擇 **prod** 分支、設定 `VITE_API_URL` 為後端 API 根網址後部署即可。
